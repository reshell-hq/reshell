/**
 * Scene logic barrel (plan 014): the Scene contract and the pure widget
 * visibility rules. The scene *components* and registry live in
 * `components/scenes/` (React); this module stays React-free (ADR-0009).
 */
export type { Scene, SceneProps } from "./types";
